language: crystal
dist: bionic
sudo: false

crystal:
  - latest
  - nightly

install: shards install

script:
  - crystal spec -Dpreview_mt

matrix:
  fast_finish: true
  allow_failures:
    - crystal: nightly

cache:
  directories:
    - lib
